package pro.sky.java.course2.simplecalculator.service;

public interface SimpleCalculatorService {
    int sumUpNumbers(int num1, int num2);
    int subtractNumbers(int num1, int num2);
    int multiplyNumbers(int num1, int num2);
    int divideNumbers(int num1, int num2);
}